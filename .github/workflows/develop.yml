name: Update Develop

on:
  workflow_dispatch:

jobs:
  ff-develop:
    runs-on: ubuntu-latest
    steps:

      - name: Create App Token
        uses: actions/create-github-app-token@v1
        id: app-token
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_TOKEN }}

      - name: Check Out Repo
        uses: actions/checkout@v4
        with:
          token: ${{ steps.app-token.outputs.token }}
          ref: develop
          fetch-depth: 0

      - name: Fast Forward Develop
        run: |
            value=$(cat VERSION)
            develop_hash=$(git rev-parse HEAD)
            next_build="${{ vars.DEVELOP_BUILD }}"
          
            if [[ "$value" == *"-"* ]]; then
                develop_build="$((${value#*-build}))"
            else
                develop_build="1"
            fi
          
            git checkout origin/nightly
            
            value=$(cat VERSION)
          
            if [[ "$value" == *"-"* ]]; then
                nightly_build="$((${value#*-build}))"
            else
                nightly_build="1"
            fi
          
            echo "DEVELOP_BUILD: '${develop_build}'"
            echo "NIGHTLY_BUILD: '${nightly_build}'"
            echo "NEXT_BUILD: '${next_build}'"
            
            if (( next_build <= develop_build )); then
                echo "NEXT_BUILD variable must be greater than DEVELOP_BUILD"
                exit 1
            elif (( next_build > nightly_build )); then
                echo "NEXT_BUILD variable must be less than or equal to NIGHTLY_BUILD"
                exit 1
            fi
            
            while : ; do
                value=$(cat VERSION)
          
                if [[ "$value" == *"-"* ]]; then
                    loop_build="$((${value#*-build}))"
                else
                    loop_build="1"
                fi
          
          
                if [[ "$next_build" == "$loop_build" ]]; then
                    hash=$(git rev-parse HEAD)
                    echo "HASH: ${hash}"
                    git push origin ${hash}:refs/heads/develop
                    break
                fi
                
                git checkout HEAD^
          
                hash=$(git rev-parse HEAD)
                if [[ "$develop_hash" == "$hash" ]]; then
                    echo "HASH: ${hash}"
                    echo "No Commit found to Fast Forward"
                    exit 1
                fi
            done
