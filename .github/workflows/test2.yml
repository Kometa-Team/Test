name: Docker Pull Release

on:
  pull_request:
    branches:
      - nightly
    types: [closed]

jobs:
  increment-version:
    runs-on: ubuntu-latest
    if: github.base_ref == 'nightly' && github.event.pull_request.merged
    steps:

      - name: Create App Token
        uses: actions/create-github-app-token@v1
        id: app-token
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_TOKEN }}

      - name: Check Out Repo
        uses: actions/checkout@v4
        with:
          token: ${{ steps.app-token.outputs.token }}
          ref: nightly
          fetch-depth: 0

      - name: Update VERSION
        run: |
            value=$(cat VERSION)
            if [[ "$value" == *"-"* ]]; then
                build_value="$((${value#*-build} + 1))"
                new_value="${value%-build*}-build${build_value}"
            elif [[ "$value" == *"!"* ]]; then
                build_value="0"
                new_value="${value%!*}"
            else
                build_value="1"
                new_value="${value%-build*}-build1"
            fi
          
            original=$(git log -1 HEAD --pretty=format:%s)
            
            echo "$new_value" > "VERSION"
            git config --local user.email "action@github.com"
            git config --local user.name "GitHub Action"
            git add VERSION
            git commit -m "[${build_value}] ${original}" --amend
            git push origin nightly --force-with-lease
